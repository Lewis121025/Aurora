version: "3.9"
services:
  aurora:
    build: .
    environment:
      - AURORA_DATA_DIR=/data
    volumes:
      - ./data:/data
    ports:
      - "8000:8000"
    command: ["bash", "-lc", "pip install -e '.[api]' && uvicorn aurora.api.app:app --host 0.0.0.0 --port 8000"]
