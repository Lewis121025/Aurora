---
description: AURORA 叙事记忆系统 - 核心开发规范
alwaysApply: true
---

# AURORA 项目开发规范

## 项目概述

AURORA 是基于叙事心理学原理设计的 AI Agent 记忆系统。核心理念：

- **记忆是身份的持续构建过程，而非过去的存档**
- 关系优先：记忆围绕"我与他人的关系"组织
- 矛盾共存：区分需要解决的矛盾与应当保留的矛盾
- 意义演化：同一事件的意义可随理解深化而改变

## 核心架构

```
Plot (原子记忆) → Story (关系叙事) → Theme (身份维度)
```

三层结构：
- **Plot**: 单次交互，包含事实层、关系层、身份层
- **StoryArc**: 围绕特定关系组织的记忆序列
- **Theme**: 从多个故事中涌现的身份维度

## 设计原则

### 1. 零硬编码阈值

```python
# ❌ 错误
if similarity > 0.8:
    merge()

# ✅ 正确 - 使用贝叶斯/随机策略
prob = self.gate.prob(features)
if self.rng.random() < prob:
    merge()
```

### 2. 确定性可重现

所有随机操作必须支持 seed：

```python
def __init__(self, seed: int = 0):
    self.rng = np.random.default_rng(seed)
```

### 3. 状态可序列化

所有核心类必须实现 `to_state_dict()` 和 `from_state_dict()`：

```python
def to_state_dict(self) -> Dict[str, Any]:
    return {"field": self.field, ...}

@classmethod
def from_state_dict(cls, d: Dict[str, Any]) -> "ClassName":
    return cls(**d)
```

## 代码规范

### 类型标注

所有公共方法必须有完整类型标注：

```python
def query(
    self, 
    text: str, 
    k: int = 5, 
    asker_id: Optional[str] = None
) -> RetrievalTrace:
```

### 文档字符串

使用三引号文档字符串，说明功能、参数、返回值：

```python
def ingest(self, interaction_text: str) -> Plot:
    """Ingest an interaction with relationship-centric processing.
    
    Args:
        interaction_text: The interaction text to process
        
    Returns:
        Plot object (may or may not be stored)
    """
```

### 数据类

优先使用 `@dataclass` 或 Pydantic `BaseModel`：

```python
@dataclass
class Plot:
    id: str
    ts: float
    text: str
    embedding: np.ndarray
```

## 命名约定

- 类名: `PascalCase` (如 `AuroraMemory`, `StoryArc`)
- 方法/函数: `snake_case` (如 `ingest`, `query`, `evolve`)
- 私有方法: `_snake_case` (如 `_store_plot`, `_compute_signals`)
- 常量: `UPPER_SNAKE_CASE` (如 `MAX_PLOTS`, `EPSILON_PRIOR`)

## 线程安全

多租户场景下使用锁保护共享状态：

```python
def __init__(self):
    self._lock = threading.RLock()

def public_method(self):
    with self._lock:
        # 操作共享状态
```

## 依赖管理

- 核心依赖: numpy, scipy, networkx, pydantic
- 可选依赖通过 extras 管理: `[api]`, `[ark]`, `[faiss]`

## 中文支持

项目同时支持中英文，文档和注释可使用中文。
